# 2026-08-01 会话记录

## 本轮：确认关键词输入分隔符

### 用户

输入框内，使用 `|` 或 `;` 分割。

### AI

已检查当前实现：输入框目前使用顿号展示，并接受顿号、中文逗号、英文逗号和换行解析。实施前需要确认改为 `|` / `;` 后，是否继续兼容旧分隔符输入。本阶段未修改业务代码。

## 本轮：明确新分隔符集合

### 用户

改为使用 `|`、`;`、英文逗号或换行。

### AI

确认解析器只接受竖线、英文分号、英文逗号和换行；中文逗号和顿号不再作为分隔符。建议输入框统一使用 `|` 回显，帮助文案同步列出四种分隔方式，底层规则生成仍使用 `|` 连接关键词。等待设计确认后实施。

## 本轮：评估标签式关键词输入

### 用户

关键词是不是使用标签，更好？

### AI

标签更符合关键词离散集合的业务模型。推荐使用“标签列表 + 尾部输入框”的组合：现有关键词显示为可删除标签；输入 `|`、`;`、英文逗号、换行或 Enter 时提交标签；粘贴批量文本时按相同分隔符拆分、去空和去重；空输入时按 Backspace 可删除最后一个标签。Pure 规则继续只读。等待用户确认该交互设计后实施。

## 本轮：确认并完成关键词标签输入

### 用户

确认。

### AI

已完成普通规则关键词标签编辑器：支持 `|`、`;`、英文逗号、换行、Enter、blur、删除按钮和空输入 Backspace；中文逗号与顿号保留为关键词正文，中文输入法组合期间不会误提交。多行粘贴会在浏览器清理换行前读取剪贴板，余留输入会启用保存并在保存前统一提交。标签使用独立容器增量刷新，避免删除、保存和连续录入时丢失内容或焦点。Pure 规则继续只读，既有规则生成、API 与安全分阶段保存逻辑保持不变。新增与更新测试共 30 项通过，并补充了帮助文案关联和保存状态播报。

## 本轮：梳理 401 登录重试需求

### 用户

参考 `frontend/登录.txt`：接口出现 401 时，弹窗提示输入用户名、密码，接口回写 Cookie 后重试。

### AI

已确认登录接口为 `/api/v2/auth/login`，使用 URL 编码表单提交用户名和密码，并需要 `credentials: include` 接收 Cookie。现有规则读取和保存请求也已携带凭据。实施前需要确认登录失败时弹窗的保留与重试交互；不会把样例文件中的账号或密码写入代码、文档或日志。

## 本轮：确认 401 登录失败交互

### 用户

继续。

### AI

按推荐交互继续设计：登录失败时保留弹窗并显示错误，允许修改凭据后再次提交；不保存密码，不从样例文件预填账号或密码。下一步提交统一认证请求、登录弹窗、单次请求重试及并发 401 合并处理的最小设计供确认。

## 本轮：确认并落盘 401 登录重试设计

### 用户

可以。

### AI

已将确认后的设计保存到 `docs/2026-08-01_401-login-retry-design.md`。设计采用统一认证请求包装器、可复用页面内登录弹窗和共享登录尝试 Promise：401 时收集凭据并调用登录接口，Cookie 回写后每次凭据提交只重试原请求一次；验证失败时保留同一弹窗继续输入；并发 401 共用一次登录尝试；取消后等待请求明确失败。设计已完成敏感信息、占位符、内部一致性、格式和 diff 自审，等待用户审阅书面设计后编写实施计划。

## 本轮：确认设计并完成实施计划

### 用户

确认。

### AI

已根据确认后的书面设计创建 `plans/2026-08-01_401-login-retry.md`。计划分为认证请求与登录弹窗控制器、规则接口接入与样式、文档审查与最终验证三个任务，采用测试先行实现，并明确共享登录 Promise 在成功后必须清理，避免重试 401 时复用已完成 Promise 造成自动循环。计划已完成需求覆盖、接口一致性、占位符、敏感信息和格式自审，等待选择执行方式。

## 本轮：完成 401 登录重试开发

### 用户

选择当前会话直接开发。

### AI

已完成规则接口 401 登录重试：读取和保存统一通过认证请求包装器；页面弹窗提交 URL 编码用户名、密码到 `/api/v2/auth/login` 并使用 `credentials: include` 接收 Cookie；登录后重试原请求，失败时保留弹窗，取消时结束同批等待并保留未保存草稿。并发 401 共享登录尝试，登录成功版本号可识别 Cookie 更新前发出的迟到 401，取消版本号可阻止同批迟到响应重新弹窗；并发请求处于不同重试阶段时不会互相关闭弹窗。弹窗打开时规则页面设为 `inert`，密码在失败、成功和取消时清空。未写入、预填或记录样例凭据。最终只读审查无 Critical/Important 问题，43 项测试通过。
